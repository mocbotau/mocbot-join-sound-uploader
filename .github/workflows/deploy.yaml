name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    uses: mocbotau/infra-workflows/.github/workflows/generic-deploy.yaml@main
    with:
      event-name: ${{ github.event_name }}
      environment: "prod"
    secrets: inherit
